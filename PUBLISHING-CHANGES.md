# 公开发布副本说明

此仓库由本地 `findMusic` 工作目录创建的独立发布副本生成。原工作目录和其中的 MIDI、乐谱、图片、脚本均未被本发布流程修改。

为适合公开发布，副本仅作了以下整理：

- 新增中文总说明、曲目目录、平台使用指南、提示词、投稿规则与许可证说明；
- 为防止泄露个人信息，将历史报告和 JSON 中的本机绝对路径改为“仓库根目录”或通用路径说明；
- 将两处依赖本机 Python 安装位置的命令改为通用 `python` 命令；
- 排除嵌套 `.git` 元数据和 Python `__pycache__` 缓存，不影响源代码或音乐内容。

公开前已逐一比较所有 43 个 MIDI 文件的 SHA-256：发布副本中的 MIDI 字节与原工作目录完全一致。

历史报告中可能仍保留其生成时的“私人使用”“权利未知”等审计判断。这些历史记录不会覆盖根目录 [RIGHTS.md](RIGHTS.md) 所述的当前公开许可范围。
