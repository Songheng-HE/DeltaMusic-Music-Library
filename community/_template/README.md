# 投稿模板

复制本目录到：

```text
community/<曲目英文标识>/candidate-<GitHub-ID>-v<版本号>/
```

在这个新目录中放入你的真实 MIDI，并填写 `submission.yml`。不要上传授权书原件、私人聊天记录或本机绝对路径。

导入设置必须如实填写：`pitch_offset_semitones: -12` 表示导入时降一个八度；默认也必须写 `0`。只有会显著改变结果的非默认设置才写入文件名，完整设置以 `submission.yml` 为准。
