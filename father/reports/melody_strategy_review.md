# 《父亲》MIDI：旋律声部与交接审阅

审阅日期：2026-09-11（Asia/Shanghai）  
输入：`input/父亲——筷子兄弟.mid`（SHA-256 `589a40d6deb5a823eeaf2ee08dd4f801e41d7ec75edae601993e20584a3a4571`）  
权利记录：社区 MIDI 来源，`unknown`；仅适合已由用户确认的私用技术处理，不能据此推定可公开发布衍生物。

## 审计事实

- SMF Type 1、PPQ 480、时长 292.703 秒、4 个速度事件；全曲 4/4、E 大调。
- `Voice`（轨 1，通道 0，亮音钢琴）有 442 个音符，音域 B3–C#6，起于 tick 15360、止于 tick 145824；最大同时音符数 1、重叠数 0。它是明确命名且严格单音的主旋律基线。
- `Piano`（轨 2）有 628 个音符、394 个起音、最大同时音符数 4、重叠数 250；`Sound 3`（轨 3）和鼓轨（轨 4）也都是复音/伴奏纹理。它们不能整体当作旋律轨，更不能按“全曲最高音”抽取。
- 完整逐轨数据见 `track_report.json` 和 `track_report.md`。

## 已确认的交接

`Voice` 的多数间隙很短：共有 6 个超过一拍的间隙，其中 5 个均为 816 ticks（1.7 拍），符合句间休止，不足以证明换声部。

唯一明显的长间隙是：

| 段落 | 起止 tick | 约时码（65 BPM 段） | 证据 |
| --- | ---: | ---: | --- |
| `Voice` 结束后 | 69984–72960 | 2:14.6–2:20.3 | 长 2976 ticks（6.2 拍）；轨 2 在其中连续出现 9 个单起音音符，tick 为 70080、70320、70560、70800、71760、72000、72240、72480、72720。`Voice` 于 tick 72960 恢复。 |

该短句内的轨 2 音高依序是 E4、G#4、D#4、E4、D#4、E4、G#4、D#4、E4。前后各留有小休止，且没有轨 2 的和弦落入这九个起音位置；这是可辩护的**短句级旋律交接**，不是在混合伴奏中作逐音筛选。

## 结论与处理选项

结构分类：

1. **A — 忠实独立声部基线**：仅提取 `Voice`，保留其 6.2 拍休止。分类为 `isolated-melody`；严格单音且不会引入编配判断。
2. **B — 单线交接编配（尚未制作）**：使用 `Voice` 作为基线，只在 tick 69984–72960 插入上述来自 `Piano` 的完整九音短句。分类为“single-line arrangement”，因为它跨越了原始声部；需要用户确认后才能制作。不得把它表述为从原文件唯一恢复出的单一轨。

本次只做分析，没有生成或抽取 MIDI。后续若确认 B，提取报告将明确列出：基线来源为轨 1；新增短句来源为轨 2；接缝、休止与九个音符的精确 tick；以及其为私用编配的权利限制。
